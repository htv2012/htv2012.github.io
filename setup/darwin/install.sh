    sh -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"    
    brew install -y 1password-cli
    brew install -y bat
    brew install -y copier
    brew install -y eza
    brew install -y fzf
    brew install -y git-delta
    brew install -y hatch
    brew install -y httpie
    brew install -y jq
    brew install -y multipass
    brew install -y ripgrep
    brew install -y tree
    brew install -y uv
